Basic:
Question:- Retrieve the total number of orders placed.
Answer:- select count(order_id) as total_orders from orders; 
question :-Calculate the total revenue generated from pizza sales.
Answer:- select sum(order_details.quantity * pizzas.price) as total_sales form order_details join pizzas on pizzas.pizza_id = order_details.pizza_id
Question:- Identify the highest-priced pizza.
Answer:- select pizza_types.name , pizzas.price form  pizza_types join pizzas on pizza_types.price_type_id = pizzas.pizza_type_id order by pizzas.price desc limit 1;
Question :- Identify the most common pizza size ordered.
Answer :- select pizzas.size , COUNT(order_details_id) AS order_count FROM pizzas join order_details ON pizzas.pizzas_id = order_details.pizzas_id GROUP BY pizzas.size ORDER by order_count DESC; 
Question:- List the top 5 most ordered pizza types along with their quantities.
Answer :- select pizzas_types.name , sum(order_details.quantity)  as quantity form pizza_types join pizzas on pizza_types.pizza_type_id = pizzas.pizzas_type_id join order_details on order_deatils.pizza_id =pizzas.pizza_id group by pizza_types.name order by quanity desc limit 5 ;


Intermediate:
Question:-Join the necessary tables to find the total quantity of each pizza category ordered
Answer :- select pizza_types.category ,   sum(order_deatils.quantity) as qunatity forom pizza_types join pizzas on pizza_types.pizza_type_id = pizzas.pizza_type_id join order_deails on order_details.pizza_id = pizza.pizza_id group by pizza_types.category order by quantity desc;
Question:-Determine the distribution of orders by hour of the day.
Answer :-  select hour(order_time) form count(order_id) from  orders gorup by hour(order_time); 
Question:- Join relevant tables to find the category-wise distribution of pizzas.
Answer :- select category , count(name) from pizza_types group by category ;
Question:-Group the orders by date and calculate the average number of pizzas ordered per day.
Answer :-select avg (quantity) from (select orders.order_date , sum(order_details.quantity) as quantity  form orders join order_deatils on orders.order_id = order_deails.order_id group by orders.order_date) as order_quantity ;
Question:-Determine the top 3 most ordered pizza types based on revenue.
Answer :- select pizza_types.name ,sum( order_details.quantity * pizzas.price) as revenue from pizza_types join pizzas on pizzas.pizza_type_id= pizza_types.pizza_type_id join order_details on order_details.pizza_id = pizza.pizza_id group by pizza_types.name order by revenue desc limit 3 ;

Advanced:
Question:-Calculate the percentage contribution of each pizza type to total revenue.
Answer :-  

Question:-Analyze the cumulative revenue generated over time.
Answer :- 
Question:-Determine the top 3 most ordered pizza types based on revenue for each pizza category
Answers:-
